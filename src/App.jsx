/**
 * App.jsx
 *
 * The root component is intentionally kept minimal.
 * Routing is fully handled by React Router via RouterProvider in main.jsx.
 * All page components are rendered through router.jsx → MainLayout → <Outlet />.
 *
 * This file exists as a placeholder so you can add
 * app-level logic here (e.g. auth initialization, theme detection)
 * without touching main.jsx.
 */

const App = () => {
  // App-level side-effects (auth checks, theme init, etc.) go here
  return null;
};

export default App;
