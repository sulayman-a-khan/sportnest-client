# auth/

This folder will hold all authentication-related components and logic.

Planned contents:
- `AuthContext.jsx`       — React context providing user state & auth methods
- `AuthProvider.jsx`      — Context provider wrapping the app
- `useAuth.js`            — Custom hook to consume AuthContext
- `LoginPage.jsx`         — Login form page
- `RegisterPage.jsx`      — Registration form page
- `ForgotPassword.jsx`    — Password reset flow

Note: Do NOT add business logic here yet. This is a placeholder.
