/**
 * hooks/ — Custom React hooks
 *
 * Place all reusable hooks here. Examples:
 *
 *  useFacilities.js   — fetch all facilities via TanStack Query
 *  useBooking.js      — booking mutation + invalidation logic
 *  useCurrentUser.js  — read authenticated user from AuthContext
 *  useDebounce.js     — debounce a value (useful for search inputs)
 *
 * Keep hooks small, focused, and testable.
 */
